# API Configuration
# The base URL for the quiz API
# IMPORTANT: In Next.js, environment variables that need to be accessible on the client-side
# must be prefixed with NEXT_PUBLIC_
# For local development, this should point to the Cloudflare Worker dev server
# For production, use your deployed Cloudflare Worker URL
API_URL=http://localhost:8787

# Example production URL:
# API_URL=https://quiz-api.your-domain.workers.dev